<?php
/**
 * Server-side rendering of the `godam/gallery-v2-item` block.
 *
 * Frontend output is intentionally deferred. The block currently stores only
 * the selected `videoId` for editor-side composition.
 *
 * @package GoDAM
 */

defined( 'ABSPATH' ) || exit;

return '';
