/**
 * The save function for the GoDAM Image block.
 * This block is dynamic, so it does not save any content.
 *
 * @return {null} Nothing — the block renders server-side via render.php.
 */
export default function save() {
	return null;
}
