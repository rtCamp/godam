/**
 * Shared artwork sources for the first-run welcome modals.
 *
 * Kept in one place so the "Make your videos interactive" chooser card and the
 * single-option "Get Started with GoDAM" intro use the same image. `null` means
 * render the empty placeholder box — drop the real image URLs here later.
 */
export const WELCOME_IMAGES = {
	interactive: null,
	woo: null,
};
